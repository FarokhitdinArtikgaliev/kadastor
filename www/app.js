const routes = [
["Фергана","Фергана: город",["Фарғона шаҳар","Марғилон шаҳар","Тошлоқ тумани"],3719,29],
["Фергана","Фергана: восток",["Қува тумани","Қувасой шаҳар","Фарғона тумани"],1803,139],
["Фергана","Фергана: север",["Ёзёвон тумани","Қўштепа тумани"],1154,55],
["Фергана","Фергана: юго-запад",["Олтиариқ тумани","Бағдод тумани","Бувайда тумани"],1661,145],
["Коканд","Коканд: город и юг",["Қўқон шаҳар","Фурқат тумани","Ўзбекистон тумани"],1980,223],
["Коканд","Коканд: север",["Данғара тумани","Бешариқ тумани","Учкўприк тумани"],1592,255],
["Фергана","Фергана: Сох",["Риштон тумани","Сўх тумани"],906,205,"Проверить режим проезда"],
["Андижан","Андижан: запад",["Улуғнор тумани","Балиқчи тумани","Бўстон тумани"],1040,142],
["Андижан","Андижан: центр",["Шаҳрихон тумани","Олтинкўл тумани","Андижон тумани"],3264,165],
["Андижан","Андижан: город",["Андижон шаҳар","Избоскан тумани"],3770,181],
["Андижан","Андижан: юг",["Асака тумани","Марҳамат тумани","Булоқбоши тумани"],1638,186],
["Андижан","Андижан: восток",["Жалақудуқ тумани","Хўжаобод тумани","Пахтаобод тумани"],1556,253],
["Андижан","Андижан: дальний восток",["Қўрғонтепа тумани","Хонобод шаҳар"],899,276],
["Наманган","Наманган: запад",["Поп тумани","Мингбулоқ тумани","Чуст тумани"],2411,252],
["Наманган","Наманган: центр",["Наманган тумани","Наманган шаҳар","Тўрақўрғон тумани"],4805,185],
["Наманган","Наманган: восток",["Уйчи тумани","Чортоқ тумани","Янгиқўрғон тумани"],1651,214],
["Наманган","Наманган: дальний север",["Косонсой тумани","Учқўрғон тумани","Норин тумани"],1948,281]
].map(([region,title,stops,workload,km,note],i)=>({id:i+1,region,title,stops,workload,km,note}));
const stopCoords = {"Андижон шаҳар":[40.7598055556,72.3589166667],"Хонобод шаҳар":[40.8019166667,72.9846666667],"Олтинкўл тумани":[40.7964722222,72.1648611111],"Андижон тумани":[40.8565277778,72.3088888889],"Балиқчи тумани":[40.9039444444,71.8457222222],"Бўстон тумани":[40.6897222222,71.9488055556],"Булоқбоши тумани":[40.6391111111,72.4958333333],"Жалақудуқ тумани":[40.7160833333,72.64375],"Избоскан тумани":[40.8970833333,72.2558888889],"Улуғнор тумани":[40.7561666667,71.7048333333],"Қўрғонтепа тумани":[40.7288055556,72.758],"Асака тумани":[40.6405833333,72.2477777778],"Марҳамат тумани":[40.5058055556,72.3438888889],"Шаҳрихон тумани":[40.7208333333,72.0372777778],"Пахтаобод тумани":[40.9358611111,72.5051388889],"Хўжаобод тумани":[40.6677777778,72.5654444444],"Наманган шаҳар":[41.0074805556,71.6675305556],"Мингбулоқ тумани":[40.8611583333,71.4582666667],"Косонсой тумани":[41.2571388889,71.5414166667],"Наманган тумани":[40.9234416667,71.5836777778],"Норин тумани":[40.9159222222,72.1189],"Поп тумани":[40.8693666667,71.11695],"Тўрақўрғон тумани":[41.0038638889,71.5094583333],"Уйчи тумани":[41.0308944444,71.8445444444],"Учқўрғон тумани":[41.1188833333,72.0744833333],"Чортоқ тумани":[41.0777222222,71.8151666667],"Чуст тумани":[40.9871388889,71.2310277778],"Янгиқўрғон тумани":[41.1915555556,71.7222777778],"Қувасой шаҳар":[40.2946388889,71.9883611111],"Қўқон шаҳар":[40.5348888889,70.9241111111],"Марғилон шаҳар":[40.4654166667,71.7166388889],"Фарғона шаҳар":[40.3946111111,71.7683888889],"Бешариқ тумани":[40.4358888889,70.6157777778],"Бағдод тумани":[40.4607777778,71.2116388889],"Бувайда тумани":[40.56025,71.1409444444],"Данғара тумани":[40.5779166667,70.9204166667],"Ёзёвон тумани":[40.5033888889,71.8544722222],"Қува тумани":[40.5376388889,72.0701944444],"Олтиариқ тумани":[40.3895277778,71.4743888889],"Қўштепа тумани":[40.5383333333,71.6454444444],"Риштон тумани":[40.3609444444,71.2942222222],"Сўх тумани":[39.9568055556,71.12925],"Тошлоқ тумани":[40.4850833333,71.754],"Ўзбекистон тумани":[40.3731111111,70.8192222222],"Учкўприк тумани":[40.5443611111,71.0540833333],"Фарғона тумани":[40.1719166667,71.7305833333],"Фурқат тумани":[40.4856666667,70.7906388889]};
const START_POINT={lat:40.4409166667,lon:71.7560277778,label:"Точка старта / возврата"};
const state={activeRegion:"Все",today:Number(localStorage.getItem("kadastr-today")||1),mapRoute:Number(localStorage.getItem("kadastr-map-route")||1),map:null,mapLayer:null};
const getDone=()=>JSON.parse(localStorage.getItem("kadastr-done")||"[]");
const getDoneStops=()=>JSON.parse(localStorage.getItem("kadastr-done-stops")||"[]");
const setDoneStops=v=>localStorage.setItem("kadastr-done-stops",JSON.stringify(v));
const allStops=()=>routes.flatMap(r=>r.stops.map((stop,index)=>({key:`${r.id}:${index}`,routeId:r.id,route:r.title,region:r.region,stop,index})));
const getLogs=()=>JSON.parse(localStorage.getItem("kadastr-logs")||"[]");
const settings=()=>({price:Number(localStorage.getItem("fuel-price")||8500),consumption:Number(localStorage.getItem("fuel-consumption")||7),rate:Number(localStorage.getItem("compensation-rate")||1000)});
const money=n=>`${Math.round(n).toLocaleString("ru-RU")} сум`;
function mapsUrl(route){const points=route.stops.map(s=>{const p=stopCoords[s];return p?`${p[0]},${p[1]}`:`${s}, Uzbekistan`});const p=new URLSearchParams({api:"1",origin:`${START_POINT.lat},${START_POINT.lon}`,destination:`${START_POINT.lat},${START_POINT.lon}`});if(points.length)p.set("waypoints",points.join("|"));return `https://www.google.com/maps/dir/?${p}`}
function navigate(id){const r=routes.find(x=>x.id===id);window.open(mapsUrl(r),"_blank");}
function renderToday(){const r=routes.find(x=>x.id===state.today)||routes[0],done=getDone().includes(r.id),ds=getDoneStops();document.querySelector("#today-title").textContent=`День ${r.id} · ${r.title}`;document.querySelector("#todayRoute").innerHTML=`<div class="stop-list">${r.stops.map((stop,i)=>{const key=`${r.id}:${i}`,ok=ds.includes(key);return `<button class="stop-row ${ok?'is-done':''}" data-stop="${key}"><span class="stop-check">${ok?'✓':i+1}</span><span>${stop}</span></button>`}).join('')}</div><p class="muted">${r.stops.length} точки · ${r.km} км · ${r.workload.toLocaleString("ru-RU")} заявлений</p><div class="route-actions"><button class="map-button" data-map="${r.id}">🚗 Навигатор</button><button class="done-button ${done?"is-done":""}" data-done="${r.id}">${done?"✓ Маршрут выполнен":"Завершить маршрут"}</button></div>`}
function renderStats(){const routeDone=getDone().length,total=routes.length,stopDone=getDoneStops().length,km=routes.reduce((s,r)=>s+r.km,0),s=settings();const fuel=km/100*s.consumption*s.price, compensation=km*s.rate, net=compensation-fuel;document.querySelector("#stats").innerHTML=`<div class="stat"><strong>${stopDone}/47</strong><span>объектов</span></div><div class="stat"><strong>${routeDone}/${total}</strong><span>маршрутов</span></div><div class="stat"><strong>${km.toLocaleString("ru-RU")}</strong><span>км план</span></div><div class="stat"><strong>${money(compensation)}</strong><span>компенсация</span></div><div class="stat"><strong>${money(fuel)}</strong><span>топливо</span></div><div class="stat"><strong>${money(net)}</strong><span>остаток</span></div>`;document.querySelector("#fuelSummary").textContent=`${s.price.toLocaleString("ru-RU")} сум/л · ${s.consumption} л/100 км · компенсация ${s.rate.toLocaleString("ru-RU")} сум/км`}
function routeHtml(r){const done=getDone().includes(r.id),c=routeCost(r.km);return `<article class="route-card ${done?"done-card":""}"><header><div><p class="eyebrow">День ${r.id} · ${r.region}</p><h3>${r.title}</h3></div><strong>${r.km} км</strong></header><p>${r.stops.join(" · ")}</p><p class="route-meta">${r.stops.length} точки · ${r.workload.toLocaleString("ru-RU")} заявлений ${done?"· ✓ выполнено":""}</p><div class="route-cost"><span>Компенсация <b>${money(c.compensation)}</b></span><span>Топливо <b>${money(c.fuelCost)}</b></span><span>Остаток <b>${money(c.net)}</b></span></div>${r.note?`<p class="route-note">⚠ ${r.note}</p>`:""}<button data-open-day="${r.id}">Подробнее</button></article>`}
function renderRoutes(){const visible=state.activeRegion==="Все"?routes:routes.filter(r=>r.region===state.activeRegion);document.querySelector("#regionFilters").innerHTML=["Все","Фергана","Коканд","Андижан","Наманган"].map(n=>`<button class="filter ${state.activeRegion===n?"is-active":""}" data-region="${n}">${n}</button>`).join("");document.querySelector("#routeList").innerHTML=visible.map(routeHtml).join("")}
function renderLog(){const logs=getLogs();document.querySelector("#logList").innerHTML=logs.length?logs.map(l=>`<article class="log-item"><strong>${l.branch}</strong><p>${l.issue}</p><small>${l.date} · ${l.status}</small></article>`).join(""):"<p class='empty'>Записей пока нет. Добавьте результат после выезда.</p>"}
function modal(html){const m=document.querySelector("#modal");m.innerHTML=html;m.showModal()}
function openDay(id){const r=routes.find(x=>x.id===id);modal(`<h2>${r.title}</h2><div class="form-grid"><p>${r.stops.map((s,i)=>`${i+1}. ${s}`).join("<br>")}</p><p class="muted">Нагрузка: ${r.workload.toLocaleString("ru-RU")} заявлений<br>Расстояние: около ${r.km} км</p></div><div class="modal-actions"><button class="cancel" data-close>Закрыть</button><button class="save" data-map="${r.id}">🚗 Навигатор</button></div>`)}
function openLog(){const opts=routes.flatMap(r=>r.stops).map(s=>`<option>${s}</option>`).join("");modal(`<h2>Новая запись</h2><form id="logForm" class="form-grid"><label>Филиал<select name="branch">${opts}</select></label><label>Работа / неисправность<textarea name="issue" required placeholder="Что сделали?"></textarea></label><label>Статус<select name="status"><option>Выполнено</option><option>Нужно запчасть</option><option>Повторный выезд</option></select></label><div class="modal-actions"><button class="cancel" data-close type="button">Отмена</button><button class="save">Сохранить</button></div></form>`)}
function openFuel(){const s=settings();modal(`<h2>Расходы и компенсация</h2><form id="fuelForm" class="form-grid"><label>Цена пропана / топлива, сум/л<input type="number" name="price" min="0" value="${s.price}"></label><label>Расход автомобиля, л/100 км<input type="number" step="0.1" name="consumption" min="0" value="${s.consumption}"></label><label>Компенсация за 1 км, сум<input type="number" name="rate" min="0" value="${s.rate}"></label><div class="modal-actions"><button class="cancel" data-close type="button">Отмена</button><button class="save">Сохранить</button></div></form>`)}
function openChecklist(){modal(`<h2>Перед выездом</h2><div class="check-list">${["Топливо / пропан","Бумага и расходники","Лента / картриджи","Инструменты","Чистящие материалы","Телефон и пауэрбанк"].map(x=>`<label><input type="checkbox"> ${x}</label>`).join("")}</div><div class="modal-actions"><button class="save" data-close>Готово</button></div>`)}
function openPriority(){const top=[...routes].sort((a,b)=>b.workload-a.workload).slice(0,5);modal(`<h2>Приоритет</h2><div class="priority-list">${top.map((r,i)=>`<div><b>${i+1}. ${r.title}</b><span>${r.workload.toLocaleString("ru-RU")} заявлений</span></div>`).join("")}</div><div class="modal-actions"><button class="save" data-close>Закрыть</button></div>`)}
function openDayPicker(){modal(`<h2>Маршрут на сегодня</h2><label>Выберите день<select id="dayPicker">${routes.map(r=>`<option value="${r.id}" ${r.id===state.today?"selected":""}>День ${r.id}: ${r.title}</option>`).join("")}</select></label><div class="modal-actions"><button class="cancel" data-close>Отмена</button><button class="save" id="saveDay">Выбрать</button></div>`)}
document.addEventListener("click",e=>{const b=e.target.closest("button");if(!b)return;if(b.dataset.tab){document.querySelectorAll(".nav-item,.panel").forEach(x=>x.classList.remove("is-active"));b.classList.add("is-active");const panel=document.querySelector(`#${b.dataset.tab}`);panel.classList.add("is-active");if(b.dataset.tab==="mapPanel"){setTimeout(()=>{if(!state.map)initMap();else state.map.invalidateSize()},50)}window.scrollTo({top:0,behavior:"smooth"})}if(b.dataset.region){state.activeRegion=b.dataset.region;renderRoutes()}if(b.dataset.map)navigate(Number(b.dataset.map));if(b.dataset.openDay)openDay(Number(b.dataset.openDay));if(b.dataset.close!==undefined)document.querySelector("#modal").close();if(b.dataset.stop)toggleStop(b.dataset.stop);if(b.dataset.planRoute)navigate(Number(b.dataset.planRoute));if(b.id==="buildPlan")renderPlanner();if(b.dataset.planStop){let s=selectedStops();const k=b.dataset.planStop;if(s.includes(k))s=s.filter(x=>x!==k);else if(s.length<2)s=[...s,k];setSelectedStops(s);renderPlanner()}if(b.id==="optimizePlan")optimizePlan();if(b.id==="nearestPlan")suggestNearest();if(b.id==="backupButton")openBackup();if(b.id==="exportBackup")exportBackup();if(b.id==="clearPlan"){setSelectedStops([]);renderPlanner()}if(b.id==="markSelectedDone"){let d=getDoneStops();for(const k of selectedStops())if(!d.includes(k))d.push(k);setDoneStops(d);setSelectedStops([]);renderPlanner();renderStats();renderToday();renderTripHistory()}if(b.id==="optimizedNav"&&b.dataset.url)window.open(b.dataset.url,"_blank");if(b.dataset.done){let d=getDone(),id=Number(b.dataset.done);d=d.includes(id)?d.filter(x=>x!==id):[...d,id];localStorage.setItem("kadastr-done",JSON.stringify(d));renderToday();renderRoutes();renderStats()}if(b.id==="newLog")openLog();if(b.id==="fuelButton")openFuel();if(b.id==="calcButton")openCalculator();if(b.id==="monthButton")openMonthSummary();if(b.id==="checklistButton")openChecklist();if(b.id==="priorityButton")openPriority();if(b.id==="changeDay")openDayPicker();if(b.id==="saveDay"){state.today=Number(document.querySelector("#dayPicker").value);localStorage.setItem("kadastr-today",state.today);document.querySelector("#modal").close();renderToday()};if(b.id==="locationButton")locate();if(b.id==="loadMapPoints")loadMapPoints();if(b.id==="mapRouteGo")mapGo()});
document.addEventListener("change",e=>{if(e.target.id==="backupFile"&&e.target.files?.[0])importBackup(e.target.files[0]);});
document.addEventListener("submit",e=>{e.preventDefault();if(e.target.id==="logForm"){const f=new FormData(e.target);const logs=getLogs();logs.unshift({branch:f.get("branch"),issue:f.get("issue"),status:f.get("status"),date:new Date().toLocaleDateString("ru-RU")});localStorage.setItem("kadastr-logs",JSON.stringify(logs));document.querySelector("#modal").close();renderLog()}if(e.target.id==="fuelForm"){const f=new FormData(e.target);localStorage.setItem("fuel-price",Number(f.get("price")));localStorage.setItem("fuel-consumption",Number(f.get("consumption")));localStorage.setItem("compensation-rate",Number(f.get("rate")));document.querySelector("#modal").close();renderStats();document.querySelector("#fuelSummary").textContent=`${Number(f.get("price")).toLocaleString("ru-RU")} сум/л · ${Number(f.get("consumption"))} л/100 км · компенсация ${Number(f.get("rate")).toLocaleString("ru-RU")} сум/км`}if(e.target.id==="calcForm"){const f=new FormData(e.target),km=Number(f.get("km")),c=routeCost(km);document.querySelector("#calcResult").innerHTML=`<div class="month-grid"><div><b>${km.toFixed(1)} км</b><small>пробег</small></div><div><b>${c.fuelLiters.toFixed(1)} л</b><small>топливо</small></div><div><b>${money(c.fuelCost)}</b><small>расход</small></div><div><b>${money(c.compensation)}</b><small>компенсация</small></div><div><b>${money(c.net)}</b><small>остаток</small></div></div>`}});

function getCoords(){return JSON.parse(localStorage.getItem("kadastr-coords")||"{}");}
function setCoords(v){localStorage.setItem("kadastr-coords",JSON.stringify(v));}
function selectedStops(){return JSON.parse(localStorage.getItem("kadastr-selected")||"[]");}
function setSelectedStops(v){localStorage.setItem("kadastr-selected",JSON.stringify(v));}
function routeCost(km){const s=settings();const fuelLiters=km*s.consumption/100,fuelCost=fuelLiters*s.price,compensation=km*s.rate;return {fuelLiters,fuelCost,compensation,net:compensation-fuelCost};}
function renderPlanner(){
 const done=getDoneStops(), pending=allStops().filter(x=>!done.includes(x.key)), selected=selectedStops();
 const c=routeCost(0);
 document.querySelector('#plannerSummary').innerHTML=`<div class="planner-kpis"><strong>${done.length}/47</strong><span>выполнено</span><strong>${pending.length}</strong><span>осталось</span><strong>${selected.length}/2</strong><span>выбрано на день</span></div><p class="muted">Выбери максимум 2 объекта. Приложение найдёт координаты, сравнит порядок и посчитает реальный дорожный маршрут.</p>`;
 document.querySelector('#plannerList').innerHTML=pending.map(x=>`<button class="planner-stop ${selected.includes(x.key)?'is-selected':''}" data-plan-stop="${x.key}"><span>${selected.includes(x.key)?'✓':x.index+1}</span><div><b>${x.stop}</b><small>${x.region} · ${x.route}</small></div></button>`).join('')+
 `<div class="planner-actions"><button class="primary-button" id="nearestPlan">📍 Ближайшие 2</button><button class="primary-button" id="optimizePlan" ${selected.length!==2?'disabled':''}>🧭 Оптимизировать 2 точки</button><button class="text-button" id="clearPlan">Сбросить</button></div><div id="planResult"></div>`;
}
async function geocodeCached(stop){const exact=pointForStop(stop);if(exact)return {...exact,name:"Точная координата из базы"};const cache=getCoords();if(cache[stop])return cache[stop];const u="https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&accept-language=ru&q="+encodeURIComponent(stop+", Uzbekistan");const r=await fetch(u,{headers:{"Accept":"application/json"}});if(!r.ok)throw new Error("geocode");const d=await r.json();if(!d[0])return null;const g={lat:+d[0].lat,lon:+d[0].lon,name:d[0].display_name};cache[stop]=g;setCoords(cache);return g;}
async function roadRoute(a,b){
 const u=`https://router.project-osrm.org/route/v1/driving/${a.lon},${a.lat};${b.lon},${b.lat}?overview=false&steps=false`;
 const r=await fetch(u); if(!r.ok) throw new Error('routing'); const d=await r.json(); if(d.code!=='Ok'||!d.routes?.[0]) throw new Error('routing'); return {km:d.routes[0].distance/1000,min:d.routes[0].duration/60};
}
async function optimizePlan(){
 const keys=selectedStops(), result=document.querySelector('#planResult'); if(keys.length!==2)return;
 const items=keys.map(k=>allStops().find(x=>x.key===k)).filter(Boolean); result.innerHTML='<div class="loading">⏳ Определяю координаты двух объектов и строю круговой маршрут…</div>';
 try{
  const a=await geocodeCached(items[0].stop), b=await geocodeCached(items[1].stop); if(!a||!b)throw new Error('Не удалось найти адрес одного из объектов');
  const start=START_POINT;
  const [ab,ba]=await Promise.all([roadRoute(a,b),roadRoute(b,a)]);
  const [sa, sb]=await Promise.all([roadRoute(start,a),roadRoute(start,b)]);
  const [as, bs]=await Promise.all([roadRoute(a,start),roadRoute(b,start)]);
  const option1={first:items[0],second:items[1],km:sa.km+ab.km+bs.km,min:sa.min+ab.min+bs.min};
  const option2={first:items[1],second:items[0],km:sb.km+ba.km+as.km,min:sb.min+ba.min+as.min};
  const best=option1.km<=option2.km?option1:option2, cost=routeCost(best.km);saveTrip({stops:[best.first.stop,best.second.stop],km:best.km,min:best.min,fuelCost:cost.fuelCost,compensation:cost.compensation,net:cost.net});
  const firstCoords=stopCoords[best.first.stop], secondCoords=stopCoords[best.second.stop];
  const navUrl=`https://www.google.com/maps/dir/?api=1&origin=${start.lat},${start.lon}&destination=${start.lat},${start.lon}&waypoints=${firstCoords[0]},${firstCoords[1]}|${secondCoords[0]},${secondCoords[1]}`;
  result.innerHTML=`<article class="optimized-card"><div class="optimized-head"><div><p class="eyebrow">Оптимальный круговой маршрут</p><h3>🏁 Старт: ${START_POINT.lat.toFixed(6)}, ${START_POINT.lon.toFixed(6)}<br>↓<br>1. ${best.first.stop}<br>↓<br>2. ${best.second.stop}<br>↓<br>🏁 Возврат на старт</h3></div><strong>${best.km.toFixed(1)} км</strong></div><p class="muted">Расстояние рассчитано по дорогам: старт → 1-я точка → 2-я точка → возврат на старт.</p><div class="cost-grid"><div><b>${Math.round(best.min)} мин</b><small>в дороге</small></div><div><b>${cost.fuelLiters.toFixed(1)} л</b><small>пропан</small></div><div><b>${money(cost.fuelCost)}</b><small>топливо</small></div><div><b>${money(cost.compensation)}</b><small>компенсация</small></div><div><b>${money(cost.net)}</b><small>остаток</small></div></div><div class="route-actions"><button class="map-button" id="optimizedNav" data-url="${navUrl}">🚗 Открыть круговой маршрут</button><button class="done-button" id="markSelectedDone">✓ Отметить 2 точки</button></div></article>`;
 }catch(e){result.innerHTML=`<div class="error-box">⚠ ${e.message||'Не удалось построить маршрут'}</div>`;}
}
function toggleStop(key){let d=getDoneStops();d=d.includes(key)?d.filter(x=>x!==key):[...d,key];setDoneStops(d);renderToday();renderPlanner();renderStats();}

function distanceKm(a,b){const R=6371,dLat=(b.lat-a.lat)*Math.PI/180,dLon=(b.lon-a.lon)*Math.PI/180,la1=a.lat*Math.PI/180,la2=b.lat*Math.PI/180;const h=Math.sin(dLat/2)**2+Math.cos(la1)*Math.cos(la2)*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(h));}
function saveTrip(trip){const x=JSON.parse(localStorage.getItem("kadastr-trips")||"[]");x.unshift({...trip,date:new Date().toISOString()});localStorage.setItem("kadastr-trips",JSON.stringify(x.slice(0,100)));}
function trips(){return JSON.parse(localStorage.getItem("kadastr-trips")||"[]");}
function renderTripHistory(){const el=document.querySelector("#tripHistory");if(!el)return;const x=trips();el.innerHTML=x.length?x.slice(0,10).map(t=>`<article class="log-item"><strong>${new Date(t.date).toLocaleDateString("ru-RU")} · ${t.km.toFixed(1)} км</strong><p>🏁 База → ${t.stops.join(" → ")} → 🏁 База</p><small>${Math.round(t.min)} мин · топливо ${money(t.fuelCost)}</small></article>`).join(""):"<p class='empty'>Сохранённых поездок пока нет.</p>";}
function exportBackup(){const data={version:2,exportedAt:new Date().toISOString(),done:getDone(),doneStops:getDoneStops(),logs:getLogs(),trips:trips(),settings:settings()};const blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`kadastr-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);}
function importBackup(file){const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(r.result);if(Array.isArray(d.done))localStorage.setItem("kadastr-done",JSON.stringify(d.done));if(Array.isArray(d.doneStops))localStorage.setItem("kadastr-done-stops",JSON.stringify(d.doneStops));if(Array.isArray(d.logs))localStorage.setItem("kadastr-logs",JSON.stringify(d.logs));if(Array.isArray(d.trips))localStorage.setItem("kadastr-trips",JSON.stringify(d.trips));if(d.settings){localStorage.setItem("fuel-price",d.settings.price);localStorage.setItem("fuel-consumption",d.settings.consumption);if(d.settings.rate!=null)localStorage.setItem("compensation-rate",d.settings.rate)}renderToday();renderRoutes();renderLog();renderStats();renderPlanner();renderTripHistory();alert("Резервная копия восстановлена.")}catch(e){alert("Не удалось прочитать резервную копию.")}};r.readAsText(file);}
function openBackup(){modal(`<h2>Резервная копия</h2><p class="muted">Сохраните историю посещений, журнал, поездки и настройки в JSON-файл.</p><div class="modal-actions"><button class="cancel" data-close>Закрыть</button><button class="save" id="exportBackup">Экспорт</button></div><label style="margin-top:12px">Восстановить из файла<input id="backupFile" type="file" accept="application/json"></label>`);}
async function suggestNearest(){const result=document.querySelector('#planResult');const pending=allStops().filter(x=>!getDoneStops().includes(x.key));const ranked=pending.map(x=>{const p=stopCoords[x.stop];return p?{...x,distance:distanceKm(START_POINT,{lat:p[0],lon:p[1]})}:null}).filter(Boolean).sort((a,b)=>a.distance-b.distance).slice(0,2);if(ranked.length<2){result.innerHTML='<div class="error-box">Все доступные точки уже выполнены.</div>';return}setSelectedStops(ranked.map(x=>x.key));renderPlanner();setTimeout(optimizePlan,0);}
function initMap(){
  const el=document.querySelector('#map');
  if(!el || typeof L==='undefined') return;
  state.map=L.map(el,{zoomControl:true}).setView([40.38,71.78],9);
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(state.map);
  state.mapLayer=L.layerGroup().addTo(state.map);
  renderMapSelect();
}
function renderMapSelect(){
  const sel=document.querySelector('#mapRouteSelect'); if(!sel)return;
  sel.innerHTML=routes.map(r=>`<option value="${r.id}" ${r.id===state.mapRoute?'selected':''}>День ${r.id}: ${r.title}</option>`).join('');
}
async function geocode(q){const exact=pointForStop(q);if(exact)return {...exact,name:"Точная координата из базы"};const u="https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&accept-language=ru&q="+encodeURIComponent(q);const r=await fetch(u,{headers:{"Accept":"application/json"}});if(!r.ok)throw new Error("geocode");const d=await r.json();return d[0]?{lat:+d[0].lat,lon:+d[0].lon,name:d[0].display_name}:null;}
async function loadMapPoints(){if(!state.map)initMap();if(!state.map)return;const id=Number(document.querySelector("#mapRouteSelect").value),route=routes.find(r=>r.id===id);state.mapRoute=id;localStorage.setItem("kadastr-map-route",id);state.mapLayer.clearLayers();const status=document.querySelector("#mapStatus");status.textContent="Показываю точные координаты и базу…";const bounds=[[START_POINT.lat,START_POINT.lon]],pts=[[START_POINT.lat,START_POINT.lon]];const base=L.marker([START_POINT.lat,START_POINT.lon]).addTo(state.mapLayer);base.bindPopup(`<b>🏁 ${START_POINT.label}</b><br><small>${START_POINT.lat.toFixed(6)}, ${START_POINT.lon.toFixed(6)}</small>`);route.stops.forEach((stop,i)=>{const p=pointForStop(stop);if(!p)return;pts.push([p.lat,p.lon]);const marker=L.marker([p.lat,p.lon]).addTo(state.mapLayer);marker.bindPopup(`<b>${i+1}. ${stop}</b><br><small>Точная координата из предоставленного списка</small><br><small>${p.lat.toFixed(6)}, ${p.lon.toFixed(6)}</small>`);bounds.push([p.lat,p.lon]);});pts.push([START_POINT.lat,START_POINT.lon]);if(pts.length>2){try{const u=`https://router.project-osrm.org/route/v1/driving/${pts.map(p=>`${p[1]},${p[0]}`).join(";")}?overview=full&geometries=geojson&steps=false`;const rr=await fetch(u);const rd=await rr.json();if(rd.code==="Ok"&&rd.routes?.[0]?.geometry)L.geoJSON(rd.routes[0].geometry,{style:{weight:5}}).addTo(state.mapLayer);}catch(e){}}if(bounds.length){state.map.fitBounds(bounds,{padding:[30,30]});status.textContent=`🏁 База → ${route.stops.length} точек → 🏁 база. Показано ${bounds.length-1} точек.`}else status.textContent="Не удалось загрузить точки."}
function mapGo(){const id=Number(document.querySelector('#mapRouteSelect').value);navigate(id)}

function locate(){const el=document.querySelector("#locationStatus");if(!navigator.geolocation){el.textContent="Геолокация недоступна";return}el.textContent="Определяю местоположение…";navigator.geolocation.getCurrentPosition(p=>{el.textContent=`GPS: ${p.coords.latitude.toFixed(4)}, ${p.coords.longitude.toFixed(4)}`},()=>{el.textContent="Не удалось получить GPS"},{enableHighAccuracy:true,timeout:10000})}

function openCalculator(){const s=settings();modal(`<h2>Калькулятор поездки</h2><form id="calcForm" class="form-grid"><label>Километраж, км<input type="number" name="km" min="0" step="0.1" placeholder="Например, 185" required></label><p class="muted">Сейчас: ${s.rate.toLocaleString("ru-RU")} сум/км · ${s.consumption} л/100 км · ${s.price.toLocaleString("ru-RU")} сум/л</p><div id="calcResult" class="calc-result"></div><div class="modal-actions"><button class="cancel" data-close type="button">Закрыть</button><button class="save">Рассчитать</button></div></form>`)}
function openMonthSummary(){const now=new Date(),y=now.getFullYear(),m=now.getMonth(),items=trips().filter(t=>{const d=new Date(t.date);return d.getFullYear()===y&&d.getMonth()===m});const km=items.reduce((a,t)=>a+Number(t.km||0),0),fuel=items.reduce((a,t)=>a+Number(t.fuelCost||0),0),comp=items.reduce((a,t)=>a+Number(t.compensation??(t.km*settings().rate)),0),net=comp-fuel;modal(`<h2>Итог за ${now.toLocaleDateString("ru-RU",{month:"long",year:"numeric"})}</h2><div class="month-grid"><div><b>${items.length}</b><small>поездок</small></div><div><b>${km.toFixed(1)} км</b><small>километраж</small></div><div><b>${money(comp)}</b><small>компенсация</small></div><div><b>${money(fuel)}</b><small>топливо</small></div><div><b>${money(net)}</b><small>остаток</small></div></div><p class="muted">В расчёт входят сохранённые поездки из планировщика.</p><div class="modal-actions"><button class="save" data-close type="button">Готово</button></div>`)}

renderToday();renderRoutes();renderLog();renderStats();renderPlanner();renderTripHistory();initMap();
